# Archive — Items résolus / obsolètes au 2026-05-03

> Snapshot des entrées du CLAUDE.md original qui sont devenues caduques (résolues, dépassées, ou périmées). Conservé pour traçabilité, **ne pas re-injecter** dans la doc active.

---

## §5.12 ☠️ Toast d'erreur OfflineBanner sur Android — RÉSOLU V3a

**Bug original** : Quand un user est offline et toggle une tâche → toast d'erreur (cohérent avec le rollback) mais le style natif Android est moche (signalé non screenshoté).

**Statut** : ✅ Résolu en V3a — `OfflineBanner` adapté dark mode (sombre + bordure jaune + text jaune au lieu du jaune flashy criard). Toast custom global via `toastConfig` aussi adapté dark/light.

---

## §6 Bugs résiduels — résolus

### `types/task.ts` cassé — RÉSOLU V2

**Bug** : ne contient pas le type Task (Metro accepte sans typecheck → tous les imports `import { Task } from '@/types/task'` résolvent silencieusement en `any`).

**Statut** : ✅ Résolu en V2 — fichier reconstruit à partir du schéma DB et des usages dans `tasks-store.ts`. Contient `Task`, `TaskComment`, `TaskCommentReactions`, `TaskStatus`, `TaskPriority`.

### `app/admin/user-form.tsx` vs `app/admin/supabase-user-form.tsx` doublons — RÉSOLU V3b

**Bug** : doublons potentiels — un audit avait identifié `supabase-user-form` comme obsolète mais les routes pointent vers `user-form`.

**Statut** : ✅ Résolu en V3b — `supabase-user-form.tsx` supprimé (404 lignes mort code, 0 référence). Le seul screen actif est `/admin/user-form`.

---

## §7 Priorité immédiate — Refonte onglet Notifications — RÉSOLU 2026-05-02/03

**Item original** :
> Refonte onglet Notifications (demande user 2026-05-02 — gros chantier) :
> - Tap sur notif : actuellement ne fait rien — doit ouvrir la cible (deep-link event/task/category) ET marquer comme lu auto.
> - Lecture complète du message (peut-être tronqué actuellement via `numberOfLines`)
> - Delete : swipe-to-delete + long-press menu (cohérent avec tâches/events).
> - Vue détail si pas de cible (notif info pure)
> - Polish : timestamp relatif, regroupement par jour, distinction lue/non-lue plus marquée, skeleton au load

**Statut** : ✅ Livré en V1.4 — refonte UX complète + toast in-app style WhatsApp + bug critique UUID résolu (cf. `hard-lessons.md` §5.22).

---

## §7 Test global v1.3 — partiellement fait

**Item original** : "Test global de v1.3 : faire tester par Syndell + Luana + autres testeurs."

**Statut** : Partiellement fait — Salvatore a fait un audit complet mai 2026 (62% des items traités). Test communauté à relancer avec la communication v1.4.

---

## §7 Liste 13 features — 11/13 livrées

**Statut au 2026-05-03** :
- ✅ #1-#9, #11, #12 livrées
- ⏸️ **#10 Quick Actions long-press icône** — gated EAS Build
- ⏸️ **#13 Widgets Home Screen** — gated EAS Build native

Les deux dernières nécessitent un EAS Build (sortie d'Expo Go). Peu prio si pas encore prêt à build prod.

---

## §6 Sauvegardes Git — tags obsolètes

**Tags originaux** :
- `v1.0-28.04.26` — premier backup, UX overhaul + critical security fixes
- `v1.1-29.04.26` — Sécurité Supabase Phase 1+2, cache fichiers Bible, JWT fix, tracking completion tasks
- `v1.2-30.04.26` — UX polish (offline banner, haptic, badge app, optimistic UI), suppression long-press, calendrier lundi

**Statut** : Plus de 10 commits depuis le 30 avril (v1.3 + v1.4 + tous les fixes audit). Le git log est désormais la source de vérité primaire pour l'historique. Cf. `roadmap.md` "Commits récents" pour le résumé.

---

## §6 "Cleanup" mentionné dans CURRENT STATE

**Item** : "6 fichiers morts supprimés (logs-store, messages-store, types/log, types/message, utils/logging, utils/i18n), liens canOpenURL fix Android 11+"

**Statut** : ✅ Fait depuis longtemps. Plus pertinent dans la doc active.
